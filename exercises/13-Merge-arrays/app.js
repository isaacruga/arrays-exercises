var chunk_one = [ 'Lebron', 'Aaliyah', 'Diamond', 'Dominique', 'Aliyah', 'Jazmin', 'Darnell' ];
var chunk_two = [ 'Lucas' , 'Jake','Scott','Amy', 'Molly','Hannah','Lucas'];

/*const mergeArrays = (firstArray, secondArray) => {
    
    let newArray = []

    for (let i = 0; i < chunk_one.length; i++) {
        newArray.push(firstArray[i]); 
    }
    
    for (let j = 0; j < chunk_one.length; j++) {
        newArray.push(secondArray[j]);
    }
        
    return newArray;
}
*/

const mergeArrays = (firstArray, secondArray) => {
    
    let newArray = []

    for (let i = 0; i < chunk_one.length; i++) {
        let firstArray = chunk
    }
    
    for (let j = 0; j < chunk_one.length; j++) {
        
    }

    newArray.push(firstArray[i] + secondArray[j]); // no entiendo porqué no se puede hacer así
        
    return newArray;
}


console.log(mergeArrays(chunk_one, chunk_two));